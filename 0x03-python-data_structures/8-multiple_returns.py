#!/usr/bin/python3


def multiple_returns(sentence):
    first_char = ""

    if sentence == "":
        firt_char == None

    first_char = sentence[0]
    str_len = len(sentence)
    new_tuple = (str_len, first_char)

    return (new_tuple)
